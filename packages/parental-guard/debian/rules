#!/usr/bin/make -f
export DH_VERBOSE = 1

%:
	dh $@

override_dh_auto_build:
	cd agent && cargo build --release --locked
	dh_auto_build

override_dh_fixperms:
	dh_fixperms
	chmod 0440 debian/parental-guard/etc/sudoers.d/parental-os
